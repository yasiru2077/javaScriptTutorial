const person = {
  name: "John",
  age: "30",
  hobbies: ["reading", "gaming"],
};

function greet(name) {
  return `Hello, ${name}!`;
}

const message = greet(`Alice`);

console.log(message);

const add = (a, b) => a + b;

add(2, 4)

